#include "tree_utils.hpp"

#include <print>

auto test_tree_utils() -> bool {
  std::println("DEBUG: {}", __PRETTY_FUNCTION__);

  // Receive prepared structure (Raw Array)
  // Organize structure
  // Display tree structure
  // Return Tree

  return true;
}
